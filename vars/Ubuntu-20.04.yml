---

redis_svc: redis-server
redis_conf: /etc/redis/redis.conf
supervisor_svc: supervisor
supervisor_confdir: /etc/supervisor/conf.d
supervisor_ext: conf
nginx_confdir: /etc/nginx/sites-available
ssl_user: ssl-cert
ssl_privatedir: /etc/ssl/private

sketchy_python_bin: python2
sketchy_python_pip: pip2

sketchy_pkgs:
    - git
    - curl
    - python2
    - python2-dev
    - python-setuptools
    - libmariadb-dev
    - libmariadb-dev-compat
    - libxslt1-dev
    - libxml2-dev
    - libpq-dev
    - libssl-dev
    - libcrypto++-dev
    - libfontconfig1
    - mysql-client
    - redis-server
    - supervisor
    - gunicorn
    - nginx
    - acl

sketchy_phantomjs_build_pkgs:
  - cmake
  - libqt5webkit5-dev
